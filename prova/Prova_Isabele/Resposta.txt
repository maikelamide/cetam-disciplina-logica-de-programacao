//Disciplina: Lógica de Programação e Algoritmos
//Curso Técnico em Inteligência Artificial
//Aluna: Isabele Albuquerque Pinheiro
Data: 22/04/2026

algoritmo "tabuada"

procedimento menu()
inicio
   escreval("1 - Adicao")
   escreval("2 - Subtracao")
   escreval("3 - Multiplicacao")
   escreval("4 - Divisao")
   escreval("Opcao: ")
fimprocedimento

funcao soma(a, b: inteiro): inteiro
inicio
   retorne a + b
fimfuncao

funcao sub(a, b: inteiro): inteiro
inicio
   retorne a - b
fimfuncao

funcao mult(a, b: inteiro): inteiro
inicio
   retorne a * b
fimfuncao

funcao divi(a, b: inteiro): real
inicio
   retorne a / b
fimfuncao

var
   opcao, num, i: inteiro
   resp: caractere

inicio

resp <- "S"

enquanto (resp = "S") ou (resp = "s") faca

   menu()
   leia(opcao)

   enquanto (opcao < 1) ou (opcao > 4) faca
      escreval("Opcao invalida: ")
      leia(opcao)
   fimenquanto

   escreval("Numero: ")
   leia(num)

   para i de 1 ate 10 faca

      se opcao = 1 entao
         escreval(num," + ",i," = ", soma(num,i))
      fimse

      se opcao = 2 entao
         escreval(num," - ",i," = ", sub(num,i))
      fimse

      se opcao = 3 entao
         escreval(num," x ",i," = ", mult(num,i))
      fimse

      se opcao = 4 entao
         escreval(num," / ",i," = ", divi(num,i))
      fimse

   fimpara

   escreval("Continuar? (S/N): ")
   leia(resp)

fimenquanto

fimalgoritmo